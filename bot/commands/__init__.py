# betting-bot/commands/__init__.py

# This file makes the commands directory a Python package.
# Command loading is now handled by load_extension in main.py using Cogs.
# Ensure all command files within this directory define a Cog class
# and an async setup(bot) function.
